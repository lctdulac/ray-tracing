#include <iostream>
#include <cmath>
#include "Vector.h"
using namespace std;

class Sphere
{
public:
    Vector center;
    double r;

    // constructeur
    Sphere(Vector c, double r) : center(c), r(r){};

    //
};